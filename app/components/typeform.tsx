import { PopupButton } from "@typeform/embed-react"

const TypeFormButton = () => {
    return (
      <PopupButton id="PCkj8eCd" className="rounded-md bg-coral-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-coral-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-600"
      >
        Enroll Now for FREE
      </PopupButton>
    )
  }

  export default TypeFormButton